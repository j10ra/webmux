// webmux-server — Fastify plugin: WebSocket terminal bridge + tmux session host.
// Public API to implement (see docs/design.md):
//   export { terminalServer } from "./plugin.js"; // fastify plugin
//   export type { TerminalServerOptions } from "./plugin.js";
export const WEBMUX_SERVER_VERSION = "0.0.1";
